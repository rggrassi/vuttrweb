import styled from 'styled-components';
import { Section } from '../../../styles/components/Profile/styles';

export const Wrapper = styled.div`
  height: 100%;
  ${Section} {
    position: fixed;
    top: 0;
    width: 100%;
  }
`